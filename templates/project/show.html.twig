{% extends 'base.html.twig' %} {% block title %}Project{% endblock %} {% block body %}
<h1>Project</h1>

<table class="table">
    <tbody>

        <tr>
            <th>Nom du projet</th>
            <td>{{ project.name }}</td>
        </tr>
        <tr>
            <th>Date de création</th>
            <td>{{ project.startDate ? project.startDate|date('d-m-Y') : '' }}</td>
        </tr>
        <tr>
            <th>Taux horaire en €</th>
            <td>{{ project.price }}</td>
        </tr>
    </tbody>
</table>

<a class="btn btn-outline-warning" href="{{ path('project_index') }}">Retour à Mes Projets</a>

<a class="btn btn-warning btn-lg" href="{{ path('tasks_show', {'id': project.id}) }}">Modifier</a> {{ include('project/_delete_form.html.twig') }} {% endblock %}