<form method="post" action="{{ path('client_delete', {'id': client.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ client.id) }}">
    <button class="btn">Supprimer</button>
</form>
<p class="text-warning">
	<small>
		<i class="fa fa-exclamation-triangle"></i>
		La suppression de ce client entrainera la suppression définitive de tous les projets ainsi que de toutes les tâches associés.</small>
</p>

