{# <div>
<form method="post" action="{{ path('user_delete', {'id': user.id}) }}" onsubmit="return confirm('Etes-vous sur de vouloir supprimer cette tâche ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ task.id) }}">
    <button class="btn btn-outline-danger">Supprimer</button>
</form>
<p class="text-warning"><small><i class="fa fa-exclamation-triangle"></i> La suppression de cette tâche est définitive.</small></p>
</div> #}