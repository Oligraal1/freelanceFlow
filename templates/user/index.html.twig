{% extends 'base.html.twig' %}

{% block title %}Mon profil{% endblock %}

{% block body %}
<div style='width: 25%; margin: 0 auto'>
<h1 class='my-5'>Mon profil</h1>
</div>
<div class="card my-4" style="width: 50%; margin: 0 auto">
	<div class="card-body">
		<h5 class="card-title">{{ user.fistname }} {{ user.name | upper }}</h5>
		<p class="card-text">{{ user.profession }}</p>
	</div>
	<ul class="list-group list-group-flush">
		<li class="list-group-item">{{ user.adresse }} <br />{{ user.zip }} {{ user.city }}</li>
		<li class="list-group-item">Tél : {{ user.phone }}</li>
        <li class="list-group-item">E-mail : {{ user.email }}</li>
        <li class="list-group-item">Siret : {{ user.siret }}</li>
	</ul>
	<div class="card-body">
		<a class="btn btn-outline-secondary" href="{{ path('home')}}" class="card-link">Page d'accueil</a>
<a class="btn btn-outline-warning" href="{{ path('user_edit', {'id': user.id}) }}" class="card-link">Modifier</a>


	</div>
</div>

{% endblock %}